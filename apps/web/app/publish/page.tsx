"use client";
import {useState} from "react";const api=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000";
export default function Publish(){const[asset,setAsset]=useState(""),[caption,setCaption]=useState(""),[state,setState]=useState("Ready"),[diagnostic,setDiagnostic]=useState("");async function publish(){const token=localStorage.getItem("instahub_access_token"),workspace=localStorage.getItem("instahub_workspace_id");if(!token||!workspace){setState("Sign in and select a workspace first.");return}const r=await fetch(api+"/v1/instagram/posts",{method:"POST",headers:{"Content-Type":"application/json",Authorization:"Bearer "+token},body:JSON.stringify({workspace_id:workspace,media_asset_id:asset,caption})});const x=await r.json();if(!r.ok){setState("Failed");setDiagnostic(x.detail??"Unable to queue post");return}setState("Queued for Instagram");setDiagnostic("Attempt "+x.id+" is running with automatic retries.")}return <main><section><p>PUBLISH NOW</p><h1>Send it to Instagram.</h1><article><label>Media asset ID<input value={asset} onChange={e=>setAsset(e.target.value)} placeholder="Choose an asset from your library"/></label><label>Caption<textarea value={caption} onChange={e=>setCaption(e.target.value)} maxLength={2200}/></label><button onClick={publish}>Publish now</button><h2>{state}</h2><small>{diagnostic}</small></article></section><style>{`body{background:#09090b;color:#fff;font-family:Arial}section{max-width:720px;margin:auto;padding:70px 24px}section>p{color:#f472b6;letter-spacing:.14em;font-size:12px}h1{font-size:50px}article{background:#18181b;border:1px solid #27272a;border-radius:16px;padding:22px}label{display:grid;gap:8px;margin:14px 0}input,textarea{background:#09090b;color:#fff;border:1px solid #3f3f46;border-radius:8px;padding:10px}textarea{min-height:160px}button{background:#ec4899;color:#fff;border:0;border-radius:8px;padding:12px 16px;font-weight:bold}small{color:#fda4af}`}</style></main>}
