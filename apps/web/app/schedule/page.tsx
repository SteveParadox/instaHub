"use client";import{useState}from"react";export default function Schedule(){const[time,setTime]=useState("");return <main><h1>Schedule a post</h1><input type="datetime-local" value={time} onChange={e=>setTime(e.target.value)}/><p>Times are stored in UTC with your browser timezone applied before scheduling.</p><button>Schedule</button><h2>Scheduled posts</h2><p>View, edit, or cancel upcoming posts here.</p></main>}